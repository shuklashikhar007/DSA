// in this problem it will be asked min operations to convert string a to string b;
// assuming s1 is the longer string 
// and s2 is shorter string
// then ans will be for s1 - deletions = n - len(LCS)
// for s2 - additions - m - Len(LCS);
