#include<bits/stdc++.h>
using namespace std;
/*
vector<int>inorder(TreeNode* root){
vector<int>res;
stack<TreeNode*>st;
TreeNode* cur = root;
while(cur != nullptr || !st.empty()){
    if(curr != nullptr){
    st.push(curr);
    curr = curr->left; // leftmost dive karte jayege ham 
    }
    else{
        curr = st.top();
        st.pop();
        res.push_back(curr->val);
        curr = curr -> right;
    }
}
    return res;
}

*/
int main(){
    // inorder traversal (left -> root -> right)

    return 0;
}